<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%level %date{MMdd-HHmm:ss.SSS} %logger{15} %message [%thread]%n
            </pattern>
        </encoder>
    </appender>

    <logger name="io.servicefabric.transport" level="info"/>

    <root level="debug">
        <appender-ref ref="console"/>
    </root>

</configuration>
